# To Run This

Start up MySQL database
Windows: WIN + R 
    open: "services.msc

Make sure MySQL is running

start up python virtual env
pipenv install
pipenv shell

login to mySQL with root user or other user with proper permissions
mysql -u root -p

Run program with
python manage.py runserver
